<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <stroke android:width="2dp" android:color="#D0CFCC" /> <!-- Border color and width -->
    <solid android:color="@android:color/transparent" /> <!-- Background color (inside the border) -->
    <corners android:radius="5dp" /> <!-- Rounded corners (adjust radius as needed) -->
    <padding
        android:left="8dp"
        android:top="2dp"
        android:right="8dp"
        android:bottom="2dp" />
</shape>
